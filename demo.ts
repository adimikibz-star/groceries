import { GroceryManager } from './src/index';\n\n// Demo: Using the Grocery Manager\nconsole.log('🛒 Grocery Shopping List Manager - Demo\\n');\n\n// Create a manager with a $100 budget\nconst manager = new GroceryManager(100);\n\nconsole.log('📋 Creating a grocery list with $100 budget\\n');\n\n// Add items to the list\nconst apple = manager.addItem('Apples', 5, 'Fruits', 2.5);\nconsole.log(`✅ Added: ${apple.name} (${apple.quantity} @ $${apple.price} each) - Category: ${apple.category}`);\n\nconst banana = manager.addItem('Bananas', 3, 'Fruits', 1.5);\nconsole.log(`✅ Added: ${banana.name} (${banana.quantity} @ $${banana.price} each) - Category: ${banana.category}`);\n\nconst bread = manager.addItem('Whole Wheat Bread', 2, 'Bakery', 3.0);\nconsole.log(`✅ Added: ${bread.name} (${bread.quantity} @ $${bread.price} each) - Category: ${bread.category}`);\n\nconst milk = manager.addItem('Milk', 1, 'Dairy', 4.5);\nconsole.log(`✅ Added: ${milk.name} (${milk.quantity} @ $${milk.price} each) - Category: ${milk.category}`);\n\nconst cheese = manager.addItem('Cheddar Cheese', 2, 'Dairy', 6.0);\nconsole.log(`✅ Added: ${cheese.name} (${cheese.quantity} @ $${cheese.price} each) - Category: ${cheese.category}\\n`);\n\n// Display full list\nlet list = manager.getList();\nconsole.log('📦 Current Grocery List:');\nconsole.log('─'.repeat(70));\nlist.items.forEach(item => {\n  const status = item.purchased ? '✓ PURCHASED' : '○ Pending';\n  const total = item.price * item.quantity;\n  console.log(`  [${status}] ${item.name.padEnd(25)} | Qty: ${item.quantity} | $${total.toFixed(2)}`);\n});\nconsole.log('─'.repeat(70));\nconsole.log(`Total Items: ${list.items.length}`);\nconsole.log(`Budget: $${list.budget}`);\nconsole.log(`Spent: $${list.totalSpent}`);\nconsole.log(`Remaining: $${manager.getTotalBudgetRemaining()}\\n`);\n\n// Get items by category\nconsole.log('🍎 Items in \"Fruits\" category:');\nconst fruits = manager.getItemsByCategory('Fruits');\nfruits.forEach(item => {\n  console.log(`  • ${item.name} (${item.quantity})`);\n});\n\nconsole.log('\\n🥛 Items in \"Dairy\" category:');\nconst dairy = manager.getItemsByCategory('Dairy');\ndairy.forEach(item => {\n  console.log(`  • ${item.name} (${item.quantity})`);\n});\n\n// Mark items as purchased\nconsole.log('\\n🛍️  Marking items as purchased...');\nmanager.markAsPurchased(apple.id);\nconsole.log(`✓ Purchased ${apple.name}`);\nmanager.markAsPurchased(bread.id);\nconsole.log(`✓ Purchased ${bread.name}`);\nmanager.markAsPurchased(milk.id);\nconsole.log(`✓ Purchased ${milk.name}\\n`);\n\n// Display updated list\nlist = manager.getList();\nconsole.log('📦 Updated Grocery List:');\nconsole.log('─'.repeat(70));\nlist.items.forEach(item => {\n  const status = item.purchased ? '✓ PURCHASED' : '○ Pending';\n  const total = item.price * item.quantity;\n  console.log(`  [${status}] ${item.name.padEnd(25)} | Qty: ${item.quantity} | $${total.toFixed(2)}`);\n});\nconsole.log('─'.repeat(70));\nconsole.log(`Total Items: ${list.items.length}`);\nconsole.log(`Budget: $${list.budget}`);\nconsole.log(`Spent: $${list.totalSpent.toFixed(2)}`);\nconsole.log(`Remaining: $${manager.getTotalBudgetRemaining().toFixed(2)}\\n`);\n\n// Remove an item\nconsole.log('🗑️  Removing bananas from the list...');\nmanager.removeItem(banana.id);\nconsole.log(`✗ Removed ${banana.name}\\n`);\n\n// Final list\nlist = manager.getList();\nconsole.log('📦 Final Grocery List:');\nconsole.log('─'.repeat(70));\nlist.items.forEach(item => {\n  const status = item.purchased ? '✓ PURCHASED' : '○ Pending';\n  const total = item.price * item.quantity;\n  console.log(`  [${status}] ${item.name.padEnd(25)} | Qty: ${item.quantity} | $${total.toFixed(2)}`);\n});\nconsole.log('─'.repeat(70));\nconsole.log(`Total Items: ${list.items.length}`);\nconsole.log(`Budget: $${list.budget}`);\nconsole.log(`Spent: $${list.totalSpent.toFixed(2)}`);\nconsole.log(`Remaining: $${manager.getTotalBudgetRemaining().toFixed(2)}\\n`);\n\nconsole.log('✨ Demo complete! You can now experiment with the GroceryManager class.');\n